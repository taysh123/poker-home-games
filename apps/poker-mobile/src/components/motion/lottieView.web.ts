// Web stub — never imports lottie-react-native (its web entry depends on an
// optional peer we don't bundle). LottieHost always renders its static `poster`
// on web, so a real LottieView is never needed here.
export function getLottieView(): any {
  return undefined;
}
