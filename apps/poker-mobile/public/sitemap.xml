<?xml version="1.0" encoding="UTF-8"?>
<!-- Q1.3 — DELIBERATELY EMPTY. app.tpoker.app is the application host and is de-indexed
     (X-Robots-Tag: noindex — see vercel.json). Nothing here should be submitted for indexing;
     the marketing site owns discovery: https://tpoker.app/sitemap.xml

     The file is KEPT rather than deleted because the SPA rewrite answers every unmatched path
     with index.html — deleting it would make /sitemap.xml return HTML with a 200, which is
     worse than an honest empty sitemap. -->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
</urlset>
