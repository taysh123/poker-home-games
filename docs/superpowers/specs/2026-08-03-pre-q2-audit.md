# Pre-Q2 full-app audit — COMPLETE as of 2026-08-05 (10/10 app sections + 6/6 security dimensions)

_Originally run 2026-08-03 as a 4-of-10-section partial. Completed 2026-08-05 in 3 checkpointed
fleet waves (owner-directed, highest-risk-first). **Jump to "Final combined report — by
severity" near the end of this document for the ranked, current-as-of-today findings list.**
Everything above that section is the historical record of how we got here (original partial run,
then three wave checkpoints) — read it for context/evidence, not as the thing to act on first._

## Coverage — read this first (historical — original 2026-08-03 partial run)

| Fleet | Status |
|---|---|
| App audit | **4 of 10 sections completed**: home/shell, study, profile/settings, landing |
| App audit — MISSING | live-money, bankroll, stats, premium, groups, onboarding — all died on the session quota limit |
| Security audit | **0 of 6 dimensions completed** — all six died before producing any analysis; transcripts hold nothing recoverable |

**The six missing app sections include four of the Q2 pillars' own surfaces** (bankroll, stats,
premium, onboarding). The exploration facts those agents were tasked to gather — the bankroll data
model, the XP/progress engine's real inputs, the onboarding state machine, the trigger registry's
wired-vs-declared state — are NOT available. Any Q2 pillar plan written now would rest on
assumption, which is the failure mode this project keeps paying for.

Raw findings (all 30, full evidence): `scratchpad/audit-findings-raw.md` in the session dir.

## Severity tally (of the 4 completed sections, as originally reported)

1 critical · 12 major · 14 minor · 3 nit. **Since resolved: the 1 critical (PR #74, 3 follow-up
disposition commits) and 4 majors (PRs #75, #76, #77, #78, merged 2026-08-05) — see the
✅ RESOLVED annotations inline below. The counts above are the ORIGINAL tally and are not
re-computed live; do not read "1 critical" as still-open.**

---

## CRITICAL — ships today, App Store compliance risk

### Account deletion 500s for any user with settlement history
`src/PokerApp.Application/Features/Auth/Commands/DeleteAccount/DeleteAccountCommandHandler.cs:47`

The handler removes memberships, received invitations and refresh tokens, and anonymises
`SessionPlayer.UserId` — but never touches **Settlements** (`PayerUserId`/`ReceiverUserId`, required
FKs with `DeleteBehavior.Restrict`), **`SessionPlayer.LinkedUserId`** (Restrict), or **group
invitations the user SENT** (`InvitedByUserId`, Restrict). `context.Users.Remove(user)` then throws
`DbUpdateException` at `SaveChangesAsync`, which `ExceptionHandlingMiddleware` does not map — so the
live app returns a bare 500.

> **CORRECTED 2026-08-04** (the fix branch retracted two details of this finding, and PR #74
> supersedes its scope): the 500's body is `"An unexpected error occurred. TraceId: …"`, and
> ProfileScreen renders that body — the user saw a trace-id string, never the client fallback
> "Failed to delete account." (every server response carries a message, so the `??` fallback
> renders only on network-level failures where no response arrives at all). And the three Restrict blockers listed above were only the seeded reproduction;
> the full model has **eight** Restrict edges to Users, now pinned with the complete
> per-behavior FK inventory in `DeleteAccountFkIntegrityTests`.

Any user whose finished session ever had settlements calculated — **exactly the engaged users** —
cannot delete their account. iOS 1.1.1 is live; **App Store Guideline 5.1.1(v) requires working
in-app account deletion**, and the Danger Zone copy promises "permanently delete your account and
all your data." There is no backend test for `DeleteAccount`.

This is a CLASS: no handler that deletes a user-owned aggregate has an integration test proving the
delete actually succeeds against the real FK graph. The guard is an integration test per
delete-path, seeded with the FK shapes that exist in production.

**Disposition: its own slice, before any Q2 pillar work.** It is a live-compliance defect on a
shipped app; Q2 is polish by comparison.

**✅ RESOLVED PR #74** (merged 2026-08-04, after 4 disposition commits + 2 fleet rounds): FK fix
+ settlement-preservation guard (scoped to sessions with recorded settlements per owner decision)
+ `AccountDeletedAt` marker for the linked-guest shape + legacy-money-row re-keying + full FK
inventory ratchet (19 edges, all delete behaviors, not just Restrict). See PR #74's commit history
for the complete arc; the "Deferred findings" section below records what was deliberately NOT
fixed in that slice.

---

## MAJOR — grouped by theme

### Money/data correctness on the front door
- **Home has no error state.** `HomeScreen.tsx:160` — three of the seven load calls have no
  `.catch`; any failure rejects the batch into a silent `catch {}`, and the hero then renders
  `stats?.totalProfitLoss ?? 0` as an animated **₪0 "Break even"** with "0 sessions". A network
  blip shows a returning user confidently wrong money. *Never `?? 0` for money* is the class rule.
  **✅ RESOLVED PR #75** (merged 2026-08-05): independent `.catch` on all seven calls; `statsError`
  distinguishes "load failed" from "genuinely zero"; hero renders an honest error state instead.
- **30s poll fetches the entire lifetime payload.** `ActiveSessionContext.tsx:25` polls
  `getMyStats` every 30s solely to find one active session; the handler returns **all** sessions
  ever (no `Take()`), and the DTO field named `RecentSessions` is actually the complete history —
  a misleading name that Home's week-P&L derivation silently depends on.

### Navigation / IA
- **Track hub ignores its `segment` param after first mount** (`TrackScreen.tsx:50`). Tab screens
  stay mounted, so Home's "Full stats" / "See all" land on whichever segment was last open. Class:
  any always-mounted tab screen honouring route params needs a param-sync effect.

### Retention honesty (the class this project keeps re-shipping)
- **Streak/freeze maintenance runs only at cold start** (`StudyContext.tsx:87`). Mobile apps live
  suspended for days; a user returning after two days sees a stale "alive" streak, never gets the
  auto-freeze their token entitles them to, and the reminder scheduler reads the stale value.
- **`streak_risk` reminder repeats nightly forever.** `reminders.ts:80` — no `fireAtMs`, so it
  schedules `{hour: 20, repeats: true}`; eligibility is only re-evaluated on app foreground. A user
  who studies Monday and never reopens gets "your streak is at risk" **every night forever**, false
  from ~day 2. This directly violates the pillar's own standing rule that no reminder may make an
  untrue claim, and unbounded nagging drives permission revocation.
  **✅ RESOLVED PR #76** (merged 2026-08-05): converted to a one-shot `fireAtMs` for today only
  (mirrors `game_day`'s pattern), skipped once today's fire hour has passed.

### Privacy
- **Opt-out-period events leak on re-opt-in after restart.** `analytics.ts:144` — the buffer keeps
  accumulating while opted out; if the app *started* opted-out and the user later opts back in, the
  fresh client's drain loop captures up to 200 events generated while sharing was explicitly OFF.
  The same-session path is correct; only the cold-start path leaks. Exactly the untested corner.

### Latent landmines behind dark flags (fix before the flag flips, not after)
- **Cloud Sync restore is invisible to the UI and gets clobbered** by the next mutation
  (`cloudSyncService.ts:144` bypasses `LocalGamesContext`); the **Auto-sync toggle is wired to
  nothing** — a false promise the day `cloud_sync` goes live.
- **Lesson lock is enforced only at the link source** (`LessonReaderScreen.tsx:45`) while all 28
  modules' full text ships in the bundle. Latent today (no route reaches a locked id), live the
  moment any new entry point forgets the source-side check.

### Product-credibility
- **Every non-Israeli user is locked to ₪ ILS.** `features.ts:46` — `currencyPrefs` is OFF in prod,
  so the finished picker, Intl formatting for 6 currencies and locale detection are all dead code
  in production. For a money tracker on a global store, this is a first-session credibility hit.
- **Reminder prefs toggles show ON when nothing can fire** (permission denied, or on web where every
  reminders call is a no-op). CLAUDE.md claims this screen drives "honest toast copy"; the screen
  contains no toast at all — a documented guarantee that is untrue exactly where it matters.

### Marketing — an honesty pin that inverted
- **The landing site denies the live iOS app.** `apps/landing/lib/stores.ts:2` — badges render
  disabled "Coming soon", `HERO.storeNote` says "Mobile apps coming soon", **and
  `__tests__/honesty.test.ts` asserts the badges carry NO href** — so the pin that once prevented
  over-claiming now *enforces under-claiming* and structurally blocks linking the live listing.
  The landing is the single indexable search entry point, so a searcher cannot reach the iOS app
  from it at all.

  ⚠️ The audit agent proposed a specific App Store ID for the fix. **Do not take that on trust** —
  the real listing URL must be confirmed by the owner before it ships.
  **✅ RESOLVED PR #77** (merged 2026-08-05): owner supplied and verified the real URL
  (`https://apps.apple.com/app/t-poker-poker-trainer/id6781109023`, used exactly as given, not
  agent-proposed); `StoreBadge.href` is now optional and present only for a store where the app
  has genuinely shipped — App Store links out for real, Google Play stays disabled (no listing).

---

## Deferred findings — recorded for the security/privacy pass, deliberately NOT fixed in the deletion slice

These surfaced while fixing the account-deletion defect (PR #74). Owner decision 2026-08-03: record,
don't fix in that slice.

### Erasure is inverted — the PII-free rows are deleted, the PII-bearing ones survive
`DeleteAccountCommandHandler` removes Settlements, which store only GUIDs and never a name. The
departing user's **display name survives in plaintext** in surfaces the handler never touches:
`ActivityLog.ActorName` and its interpolated `Description`, `HandRecord.WinnerName`, and other
users' `Notification` bodies (e.g. `MarkSettlementPaidCommandHandler` writes
`"{payerName} marked a settlement of … as paid."` into the counterparty's inbox). `ActivityLog`'s
actor column is an unmapped plain `Guid`, so nothing cascades. Given the Danger Zone copy promises
"permanently delete your account and all associated data" and iOS is live under 5.1.1(v), this is a
compliance-adjacent gap, not only a tidiness one. Either scrub those strings on deletion or stop
implying they are covered.

### A refusal that names an action the API does not expose
Group owners still cannot delete their account. The refusal reads "Transfer ownership or delete the
groups before deleting your account" — but **no transfer endpoint exists**;
`Group.TransferOwnership` is called from exactly one place, `LeaveGroupCommandHandler`, as a side
effect of leaving. On a path Apple requires to be completable in-app, the instruction points at a
control the product does not have. That is its own honesty defect (a promise of a capability that
does not ship), separate from the compliance question of whether owners should be able to delete
at all.

### RemovePlayer/AddBuyIn race mints orphaned money rows (pre-existing, live today)
Found 2026-08-04 by the PR #74 disposition fleet, demonstrated end-to-end with the real handlers:
`BuyIn/CashOut.SessionPlayerId` is **ON DELETE SET NULL**, and `RemovePlayerCommandHandler`'s
Active branch deletes a seat's money rows from lists read before its transaction — a buy-in
committed concurrently by another group member (two devices at one table) survives the
`RemoveRange` and is SET-NULL'd by the seat delete, yielding a `(SessionPlayerId=null,
UserId=null)` row with **no account deletion involved**. A same-class variant exists on the
Draft branch (concurrent `AddBuyIn` auto-starts the session; that branch deletes the seat
without touching money rows — hits registered players too). On the live build the orphaned
amount is **silently dropped** from every calculation; after PR #74 the session is loudly
refused instead (the refusal message was reworded during disposition so it does not claim a
deletion it cannot prove). Fix belongs in `RemovePlayerCommandHandler` (transactional removal +
deleting the SET-NULL'd rows before commit, both branches) — **its own slice**, not part of the
deletion PR.
**✅ RESOLVED PR #78** (merged 2026-08-05): the Active branch's buy-in/cash-out cleanup (unchanged
`ToListAsync`+`RemoveRange`) is unified into one block that runs for BOTH branches, positioned
after both guard clauses instead of nested inside either — the Draft branch previously had none
at all. Honest limit stated in the handler: this does not add locking, so the pre-existing
narrow read-then-remove window on the Active branch is unchanged; closing that fully needs
serializable isolation or locking coordinated with `AddBuyIn`, out of scope for that fix.

### Deferred alternative to the settlement guard
The chosen fix (PR #74) refuses to recalculate a session containing a deleted player, which
prevents the data loss without touching the schema. The richer option — making
`Settlement.PayerUserId`/`ReceiverUserId` nullable so the departed party is anonymised rather than
removed, preserving the debt and its paid status — needs a migration plus a DTO change reaching the
mobile client. Deferred by owner decision 2026-08-03; revisit if the richer behaviour is ever
needed.

## What is missing, and why it blocks the Q2 plan

Not run at all: **live-money** (the app's stated critical path), **bankroll** (Q2 pillar A),
**stats** (pillar B), **onboarding** (pillar C), **premium** (pillar D), **groups**, and the
**entire security audit** (6 dimensions, including the IDOR/authorization sweep that matters most
before the API surface grows).

Silence in this document about those areas is **absence of evidence, not evidence of absence**.

**UPDATE 2026-08-05 — security fleet + missing sections now running, in 3 checkpointed waves of
3-4 HIGH-effort agents each (highest risk first), per owner instruction after the quota deaths
recorded above. Progress tracked in the table below; findings appended per-wave as each
completes, not held to the end.** This document's ORIGINAL "4 of 10" framing above is now
stale — see the live coverage table immediately below for current status.

## Coverage — LIVE (all waves complete as of 2026-08-05 — see "Final combined report" below)

| Dimension | Status |
|---|---|
| App audit: home/shell, study, profile/settings, landing | ✅ done (original run, 2026-08-03) |
| App audit: live-money | ✅ done (Wave 1, 2026-08-05) |
| App audit: bankroll | ✅ done (Wave 1, 2026-08-05) |
| App audit: stats | ✅ done (Wave 2, 2026-08-05) |
| App audit: groups | ✅ done (Wave 2 retry, 2026-08-05) |
| App audit: premium | ✅ done (Wave 3 retry, 2026-08-05) |
| App audit: onboarding | ✅ done (Wave 3, 2026-08-05) |
| Security: auth/authz + IDOR | ✅ done (Wave 1 retry — first attempt failed silently, see note below) |
| Security: API/backend | ✅ done (Wave 1, 2026-08-05) |
| Security: privacy | ✅ done (Wave 2, 2026-08-05) |
| Security: secrets | ✅ done (Wave 2, 2026-08-05) |
| Security: mobile client | ✅ done (Wave 3, 2026-08-05) |
| Security: dependencies/supply-chain | ✅ done (Wave 3, 2026-08-05) |

**Fleet reliability note (final — 3 incidents across 3 waves, 12 dimensions, 3 different failure
shapes):** Wave 1's auth/authz+IDOR agent and Wave 2's groups agent both returned a bogus
placeholder (`{"dimension":"test","findings":[]}`) after burning a full tool-call budget doing
apparently real work (45 and 31 tool calls respectively). Wave 3's premium agent hit a genuine
StructuredOutput retry-cap error (5 failed calls, no output at all) after 64 tool calls. All
three were caught before checkpointing — every wave's raw output is read in full, never trusted
from the summary line — and all three retried solo successfully on the first attempt, the third
one surfacing what turned out to be the audit's most significant premium-tier finding (the
entitlement cache bypass, below). Recorded in memory
(fleet-placeholder-result-failure-mode.md): **never trust an "N/N returned" summary line — a
placeholder or an outright error both count as a "successful" agent unless the raw per-dimension
output is actually read.** 3 incidents in 12 dimensions (25%) is a real, load-bearing rate for
this kind of fleet, not a one-off.

---

## Wave 1 findings (2026-08-05) — auth/authz+IDOR, API/backend, live-money, bankroll

Severity scale for this security+audit pass (distinct from the critical/major/minor/nit scale
used in the ORIGINAL 4 sections above): **critical** = exploitable now, live-money or full-account
compromise · **high** = real risk, needs a specific precondition · **medium** = defense-in-depth
gap · **low** = hardening opportunity · **informational** = worth knowing, not urgent. Roughly:
critical≈critical, high≈major, medium/low≈minor, informational≈nit.

### HIGH

**Any authenticated user can attach ANY other registered user to a session's ledger without
consent, then generate real settlement debts against them, with no recourse to leave**
`src/PokerApp.Application/Features/Sessions/Commands/AddPlayer/AddPlayerCommandHandler.cs:54-71`,
enabled by the unscoped `SearchUsersQueryHandler` (`src/PokerApp.Application/Features/Users/Queries/SearchUsers/SearchUsersQueryHandler.cs:10-19`)
and raw client-controlled `UserId` in `SessionsController.cs:311`.
Any user can create a standalone session (no group required), search for ANY registered user by
username substring (zero group-membership scoping), add them by Guid to the session's ledger with
only an "already added" check, record buy-ins/cash-outs against them, and `CalculateSettlements`
persists a real `Settlement` row naming the victim — which the victim then sees in their own
pending-settlements list and gets notified about when "paid". `RemovePlayerCommandHandler`
requires the CALLER to be the session creator or a group admin — the **victim has no path to
remove their own forced seat**, and once the session is Active, registered-player removal is
blocked entirely until Finished. This bypasses the consensual `SessionInviteToken` system
(single-use, 24h) that exists for exactly this purpose. **Fix:** route registered-user additions
through the invite-token flow only (or a Pending/Invited SessionPlayer state requiring accept),
and let the target remove their own seat regardless of session status. Scope `SearchUsersQuery`
to shared-group members.

**`RedeemTopUp` grants paid AI credits with no purchase-receipt verification — a dormant
credit-forgery bug behind a config flag, not a code guard**
`src/PokerApp.Application/Features/Billing/Commands/RedeemTopUp/RedeemTopUpCommand.cs:36-52`;
`TopUpCatalog.cs:14-20`; `TopUpSettings.cs:16`.
Unlike `ValidatePurchaseCommandHandler` (which calls `IBillingVerifier.VerifyAsync` — real Apple
JWS / Google OIDC / Stripe HMAC verification), `RedeemTopUpCommandHandler` only does a config
lookup on the client-supplied `ProductId` and grants credits unconditionally, keyed by an
idempotency string built from the client-supplied `PurchaseToken` — nothing ties that token to a
real purchase. Inert TODAY only because `TopUpSettings.Enabled` defaults `false` with an empty
bundle list in every appsettings file; the moment top-ups are enabled for app-store billing
launch, any authenticated user can mint free AI credits repeatably (any new token string is a new
idempotency key). **Fix:** call `IBillingVerifier.VerifyAsync` before granting, same as
`ValidatePurchase`, using the store-confirmed transaction id as the idempotency key — before
`TopUpSettings.Enabled` is ever flipped true anywhere. Consider a startup fail-loud check
(mirroring the existing prod billing/CORS `LogCritical` pattern) refusing to boot with
`Enabled=true` while no verifier call exists in the redeem path.

**`EndSession` has no concurrency guard — concurrent end-game submits duplicate `CashOut` rows and
corrupt settlement totals**
`src/PokerApp.Application/Features/Sessions/Commands/EndSession/EndSessionCommandHandler.cs:38-59`.
`Session` carries no concurrency token anywhere (repo-wide grep of Configurations for
`IsConcurrencyToken`/`IsRowVersion`: zero hits), so the read-check-then-mutate pattern against
`session.Status` is a real TOCTOU window — two concurrent "End Game & Settle" submits (two admins,
or a client retry after a timeout while the original request is still in flight) can both pass
the `Status == Active` guard and each insert a full set of `CashOut` rows from `FinalStacks`.
`CalculateSettlements`/`GetSessionBalances` sum ALL `CashOut` rows per seat with no dedupe, so the
duplicate directly doubles a player's counted cash-out. The same TOCTOU class PR #78 fixed for
`RemovePlayer`, a different instance, not yet hit. **Fix:** add a concurrency/version column to
`Session` (EF `[Timestamp]`/rowversion) so the losing writer gets a `DbUpdateConcurrencyException`
→ 409; or make `FinalStacks` idempotent per session (reject if any `CashOut` already exists when
`FinalStacks` is non-empty).

### MEDIUM

- **Webhook endpoints unauthenticated with no rate limiting, unbounded raw-body reads before
  signature check.** `WebhooksController.cs:20-79` — Stripe/Paddle/RevenueCat read the full body
  into a string before any HMAC/JWS check; no `[EnableRateLimiting]`, no `[RequestSizeLimit]`
  (Kestrel's ~28.6MB default applies). Add a per-IP rate limit + explicit request-size cap.
- **`AddBuyIn` auto-starts a Draft session for any group member, bypassing the admin/owner-only
  Start rule.** `AddBuyInCommandHandler.cs:22-39` vs `StartSessionCommandHandler.cs:22-34` — the
  latter enforces `GroupRole.Member` cannot start a session; the former performs the identical
  `Draft → Active` transition via `session.Start()` with no role check at all. Extract the role
  check into one shared enforcement point.
- **Buy-in/cash-out/hand-record authorization is scoped to the whole group, not session
  participants or a host role.** `AddBuyInCommandHandler.cs`/`AddCashOutCommandHandler.cs`/
  `AddHandRecordCommandHandler.cs:22-29` — any group member (any role) can record money against
  ANY player in that group's session, including players they're not seated with. May be
  deliberate ("any member can scorekeep") but should be a documented decision, not an emergent
  property of a read-screen `hasAccess` helper reused for money writes.
- **Session invite-token redemption is not atomic — two concurrent joins can both consume a
  "single-use" token.** `JoinSessionByTokenCommandHandler.cs:21-49` — no concurrency token, only
  a unique index on the token string itself. Same TOCTOU class as `EndSession` above; fix as a
  conditional `UPDATE ... WHERE UsedAt IS NULL` checking rows-affected.
- **Bankroll `AsyncStorage` is unscoped by user — session data crosses login boundaries on shared
  devices.** `bankrollStore.ts:20` — single hardcoded key `tpoker.bankroll.v1`, never cleared or
  namespaced on logout (`AuthContext.clearSession()` doesn't touch it). User A's venue names/
  notes/P&L are visible to User B on the same device after a account switch. Broader exposure
  than the equivalent, documented-as-ephemeral local-games gap, since Bankroll is positioned as
  personal financial tracking and reachable from the signed-in tab bar.

### LOW

- **Authenticated billing endpoints and `ChangePassword` have no rate limiting.**
  `BillingController.cs:26-58`, `AuthController.cs:150-161` — only login/register/refresh/
  coach-analyze have `[EnableRateLimiting]`. A stolen-but-valid access token allows unlimited
  `CurrentPassword` brute-force attempts.
- **`LogoutCommandHandler` revokes whatever refresh token is supplied without checking it belongs
  to the caller.** `LogoutCommandHandler.cs:11-27` — not independently exploitable (possessing
  another user's plaintext refresh token is already full compromise) but inconsistent with every
  other handler's ownership-check pattern; tighten as defense-in-depth.
- **Settlement tie-break ordering differs between the TS local engine and the C# server engine.**
  `local/settlements.ts:51-54` sorts ties by `playerId.localeCompare`; `SettlementCalculatorService.cs:9-19`
  has no secondary sort key. Totals always match; the *specific* pairing for tied balances can
  legally differ between a synced local game and the server calculation. Add the same secondary
  key server-side + a tied-balance fixture to the shared pinned test set.
- **`AddPlayer`'s duplicate-guest-name check has the same read-then-insert race as the findings
  above.** `AddPlayerCommandHandler.cs:36-52` — two concurrent "Add Guest" taps with the same name
  can both pass the pre-check and both insert. Add a unique index on `(SessionId, GuestName)`.
- **Advanced bankroll analytics ships fully built and free while `premium/config.ts` still calls
  it unbuilt, with no entitlement gate to retrofit.** `premium/config.ts:74-91` marks
  `advanced_bankroll` `comingSoon: true`; `BankrollScreen.tsx:181-208` already renders the exact
  feature (variance/drawdown/histogram), free, with zero `useEntitlements`/`LockNudge` call
  anywhere in the bankroll tree. No live harm today (flag off in prod, nothing chargeable) but the
  comment is already false and there's no gate to flip on before Bankroll goes live in prod.

### INFORMATIONAL

- CSP is `Content-Security-Policy-Report-Only`, not enforced (`SecurityHeaderPolicy.cs:18`) —
  flagged in its own doc comment as intentional pending tuning; confirm there's a follow-up.
- `JwtService.GenerateAccessToken` re-derives the signing key directly from `settings.SecretKey`
  instead of routing through `JwtKey.ResolveSigningKey` (used only on the validation side) — not
  currently exploitable (Program.cs's fail-closed check runs before any token is ever signed) but
  a future refactor could let signing and validation silently diverge.
- Apple Sign-In skips nonce replay-protection entirely when the client omits the nonce field
  (`AppleAuthService.cs:54-58`) — not independently exploitable (Apple's identity token can't be
  forged) but nonce isn't enforced as required on `AppleLoginCommand`; make it `NotEmpty()`.
- `GroupRole.Owner` vs `Groups.OwnerId` divergence (an open question from the original audit,
  line 38-40 above) — **verified NOT reachable**: exactly two call sites ever write either value,
  both mutate both columns together in the same handler before one `SaveChangesAsync`. Consider a
  structural pin (SaveChanges interceptor or DB check) so this stays true as the codebase grows.
- Tournament `rebuyCount`/`addOnCount` are hardcoded to 0-or-1 regardless of actual count entered
  (`LogSessionScreen.tsx:122-123`) — write-only today (no read site found), but any future feature
  reading these fields will silently get wrong data for every historical row logged so far.

**What Wave 1 could NOT verify** (honesty per agent, consolidated): none of the four agents ran
the app or exercised a live/deployed endpoint (static code reading only, per the defensive-review
constraint); rate-limit window/threshold values (not just attribute presence) weren't reviewed;
Google's JWKS caching lives inside a third-party package and wasn't read; not every one of the
~60 handlers' validators were opened individually (money-critical and ID-taking ones were,
low-risk GUID-only ones were spot-checked); no concurrency-race finding was reproduced under real
load (derived from tracing the read-check-write sequence + confirming no concurrency token
exists, not from an executed race); EAS beta distribution numbers (real blast-radius sizing for
the bankroll cross-account leak) weren't available.

---

## Wave 2 findings (2026-08-05) — privacy, secrets, stats, groups

### HIGH

**Deleted-account GUID (`HandRecord.CreatedByUserId`) survives forever and is served live via the
hand-history API — not just a DB residue**
`src/PokerApp.Domain/Entities/HandRecord.cs:10` (bare `Guid`, no navigation property, no FK
configured — invisible to `DeleteAccountFkIntegrityTests`' EF-model-based ratchet);
`GetSessionHandHistoryQueryHandler.cs:36` returns it verbatim in `HandRecordDto`. Stronger than
the already-documented "display name survives" gap — this is the exact original account GUID,
reachable over the network by any session participant, forever. **Fix:** null it out on deletion
(mirroring `BuyIn`/`CashOut.UserId`) and stop returning it once the referenced user no longer
exists. Extend the FK ratchet to also flag `*UserId`-named `Guid`/`Guid?` properties with no
modeled FK, so loose references like this one (and `Session.CreatorId`, `ActivityLog.ActorUserId`
— also confirmed unaddressed) can't keep escaping review.

**Privacy policy's account-deletion promise is contradicted by actual handler behavior**
`apps/poker-mobile/public/privacy.html:128-131` promises deletion removes "all associated
personal data … immediately." Confirmed false for `ActivityLog.ActorName`/`Description`,
`HandRecord.WinnerName`, other users' `Notification` bodies (already known), plus
`Session.CreatorId`, `ActivityLog.ActorUserId`, `HandRecord.CreatedByUserId` (raw ids, newly
confirmed). **Fix:** either close the gap (owner's deferred call, recorded above) or rewrite the
policy's deletion section to state the real scope honestly, per this repo's own copy-honesty
rule 1 (a claim must be true for the audience reading it).

**Analytics opt-out is not retroactive on re-opt-in — events buffered while opted out get sent in
bulk once the user opts back in**
`apps/poker-mobile/src/utils/analytics.ts:128-159` (`startClientIfAllowed`, `dispatch`),
`:211-226` (`setAnalyticsOptOut`). Confirmed still live and unpatched (this doc already flagged
it once, 2026-08-03) — traced end to end: a user who opens the app already opted-out never
constructs a PostHog client, so `dispatch()`'s `if (!gateNow()) return` leaves buffered events
undrained; opting back in constructs the client for the FIRST time and its drain loop sends
*everything* accumulated since launch, including the opted-out window. The existing test suite
doesn't catch this because it pre-creates the client via `grantAnalyticsConsent()`, which takes a
different, non-leaking path. **Fix:** on `setAnalyticsOptOut(true)`, advance `drained` to
`buffer.length` immediately so nothing generated after that point can ever be sent later. Add a
test for the specific "already-opted-out, client never started" precondition.

**`EndSession` 500s on an already-successful session end if achievement evaluation throws**
`src/PokerApp.Application/Features/Sessions/Commands/EndSession/EndSessionCommandHandler.cs:72-98`
— `session.End()` commits at line 72; the unguarded `achievementEvaluator.EvaluateAsync` call at
line 75 has no try/catch, unlike the notification block three lines later which is explicitly
wrapped ("notifications are non-critical"). A DB unique-constraint race on `(UserId,
AchievementKey)` (two EndSession calls for the same user's two different sessions) throws
`DbUpdateException`, which `ExceptionHandlingMiddleware` doesn't map — a bare 500 for a request
whose core effect already durably succeeded. **This is the identical defect class the two most
recent commits on this branch (before this fleet) just fixed elsewhere** — a best-effort side
effect throwing and taking down an already-succeeded critical-path response. **Fix:** wrap
`EvaluateAsync` in the same try/catch pattern already used for notifications one line below it.

### MEDIUM

- **Real JWT signing secret + DB password committed to git history** (commits 49cf1ed→ce5fd9d,
  ~20 commits, removed from tracking in `e6f0867` but permanently recoverable via `git log --all`
  by anyone who clones the repo). The removal commit's own message ("contained real secrets")
  confirms the team already knew. **Fix:** confirm this dev secret was never reused as the real
  Railway `JwtSettings__SecretKey` (rotate immediately if so); if the repo is ever made public or
  shared externally, scrub with `git filter-repo`/BFG first — deleting from HEAD does not remove
  it from history.
- **`AchievementEvaluator` identifies "the just-ended session" by `CreatedAt` ordering, not by
  the `sessionId` parameter it was given.** `AchievementEvaluator.cs:82-84,101-104,125,129-131` —
  `marathon`/`cash_out_even`/`comeback` all assume `finishedSessions[0]` (ordered by session
  *creation* time) is the session that was just finished; if a user finishes sessions out of
  creation order (a realistic workflow: create A, leave Active, create+finish B, later finish A),
  these three achievements evaluate against the WRONG session's numbers. Only
  `buyInsForSession` correctly keys off the passed-in `sessionId` — the inconsistency within the
  same method is itself evidence this is unintentional. A sibling drifted-duplicate streak
  calculation was also found (missing the loss-streak decrement branch present in
  `GetMyStatsQueryHandler`) — same un-consolidated-math class this project has been burned by
  before (rankRarity). **Fix:** look up the session directly by `sessionId` instead of assuming
  index 0; extract the shared streak computation into one tested helper.
- **"XP is MONOTONIC" is violated in practice** — two of `computeXp`'s terms
  (`bankrollSessions`, `localGamesFinished`) are backed by user-deletable arrays (bankroll session
  delete is a hard `filter()`; local-game delete is tombstoned and filtered from the live set),
  and the only ratchet (`lastXp`) guards the rank-up celebration, not the displayed `xpTotal`
  itself — so deleting a finished bankroll session or local game can make a user's displayed XP
  go DOWN, contradicting CLAUDE.md's explicit pin and the code's own header comment.
  `EngagementContext.test.tsx` (written specifically to pin the monotonic-XP invariant) doesn't
  cover this axis — its mock signals are static across all three tests. **Fix:** track a
  separate lifetime-counter that only increments (never reads live array length), or clamp
  displayed `xpTotal` to `Math.max(xpTotal, lastXp)`.

### LOW

- **Group/rivalry period filters bucket by `Session.CreatedAt`, not by when the session was
  actually played.** `GetGroupLeaderboardQueryHandler.cs:25-37` — a session created well before
  game night, or left in Draft/Active a long time before finishing, gets bucketed into "this
  week" by creation date, not play date. Systemic — `GetMyStatsQueryHandler.cs:94` and
  `GetWeeklyDigestQueryHandler.cs:84` use the identical cutoff, so this may be a deliberate,
  consistent design choice rather than an oversight; worth an explicit owner decision either way.

### INFORMATIONAL

- Expo push service (exp.host) receives other users' plaintext usernames (via notification
  bodies) as an undisclosed third-party recipient — not named in `privacy.html`'s service-
  provider list. Add it, or send generic push bodies and let the client fetch details in-app.
- "18+" claim has zero technical enforcement anywhere (no DOB field, no age gate) — purely
  marketing/policy copy. Product decision, not urgent from a pure data-protection standpoint.
- No automated "download my data" export endpoint — the privacy policy's access-request flow is
  entirely manual email. Not a bug at this scale; worth a self-serve export before volume makes
  manual handling unsustainable.
- `GetGroupRivalsQuery` shows pairwise head-to-head P&L to any group member, including one who
  never played with either party in the pair — a more granular disclosure than the leaderboard's
  per-person totals (which are already group-wide). Likely intended for a friend-group product;
  confirm with owner.
- CLAUDE.md's Invite Systems table calls `GroupInviteLink` "Permanent" — it actually expires
  after 7 days (`GroupInviteLink.cs:20`). The frontend already shows the real expiry correctly;
  only the doc is stale. **Fix the doc table.**
- `GroupsController`'s `LeaveGroup` XML doc comment ("The group owner cannot leave") contradicts
  the actual, correct handler behavior (owner leaving auto-transfers or deletes the group).
  **Fix the doc comment.**
- `RemoveMember` never touches the removed member's `SessionPlayer`/`BuyIn`/`CashOut` rows — their
  historical results keep appearing in the group's leaderboard/rivals forever, with no way for
  the removed member (locked out of every group query) to see or contest it. Likely deliberate
  (game results are historical fact); worth documenting explicitly if so.

**What Wave 2 could NOT verify** (consolidated): none of the four agents ran the app or a live
database (static reading only); the git-history secret scan was sampled, not exhaustive (a full
`git log -p --all` grep timed out in the sandbox — found via the removal commit's own message,
not exhaustive history scanning, so other historical secrets could exist unfound); PostHog's
actual data-processing agreement/region guarantees weren't verified beyond the `eu.i.posthog.com`
host string in code; Railway/Postgres backup encryption-at-rest and retention policy is
infrastructure config outside this repo and wasn't reviewed; no concurrency/race finding was
reproduced under real load; Groups has zero backend tests (confirmed known gap) so nothing here
is pinned against regression.

---

## Wave 3 findings (2026-08-05) — mobile client, dependencies/supply-chain, premium, onboarding

### HIGH

**axios (the shared HTTP client for the ENTIRE app, including auth token refresh) is on a version
with a High-severity advisory, plus multiple moderate ones, all fixed upstream**
`apps/poker-mobile/package.json:26` ("axios": "^1.16.1"). `npm audit` flags 10 advisories in
range >=1.0.0 <1.18.0, most moderate, one High: GHSA-gcfj-64vw-6mp9 ("Axios Node HTTP adapter can
use an inherited proxy after interceptor config cloning," range >=1.15.2 <1.18.0). CLAUDE.md's
own "Axios (converged)" architecture note confirms this single instance backs every API module
including the 401-refresh-and-retry interceptor — this touches the token path directly. Fix is
in-range (`^1.16.1` already permits 1.18.0+) — the lockfile just needs regenerating. **Fix:**
`npm install` to pick up 1.18.0+, then re-run `npx tsc --noEmit` and `npx jest` per the repo's
own pre-commit gates. Not applied in this audit — read-only, per the owner's instruction; needs
its own slice.

**Client entitlement resolver trusts an unsigned local cache whenever the server call fails,
letting a user self-grant "premium" that unlocks the client-only-gated full lesson library**
`apps/poker-mobile/src/features/premium/entitlementResolve.ts:25-37` falls back to a plain,
unsigned AsyncStorage-cached `isPremium` flag (`tpoker.entitlement.v1`, no server correlation)
whenever `/api/entitlements` fails for ANY reason — including a missing token or a network
blip, both routine, not edge cases. Two independent paths can poison that cache: (a) direct
storage edit (trivial via browser devtools on web), or (b) the always-succeeding
`mockBillingProvider` (active whenever Paddle/RevenueCat keys are unset — true in prod today),
reachable through `LandingScreen.tsx`'s purchase button, which — unlike `PaywallScreen` — has no
internal `isFeatureEnabled('paywall')` self-guard; only unreachable today because the parent
route registration itself requires `paywall=true` (false in PROD/BETA). Once poisoned, unlock is
immediate and total: `LessonReaderScreen.tsx` (the screen that actually serves lesson text)
performs **no entitlement check of its own** — the only gate is a conditional `onPress` in the
calling screen — and the full premium lesson content (22 of 28 modules) already ships unencrypted
in the app bundle regardless of tier, so "unlocking" needs no additional network fetch. Cloud
Sync and AI Coach are NOT affected — both re-verify server-side independently on every real
operation; this is specific to purely client-gated content, which today is the full lesson
library. **Fix:** never let a locally-cached plan flip `isPremium` true without a fresh, recent
server confirmation (short TTL + reconfirm on cold start; treat "server unreachable" as free, not
"trust the last local claim"). `LessonReaderScreen` should re-derive lock state itself
(`lessonAvailability`/`useEntitlements`) rather than trusting the caller's gate, matching how
`PackDetailScreen`/`StudyScreen` already do it. Separately, give `LandingScreen` the same
`isFeatureEnabled('paywall')` self-guard `PaywallScreen` already has, and gate the mock billing
provider behind a non-production build check so it can never grant a real persisted entitlement
outside development.

### MEDIUM

- **Group/session invite deep links auto-join with zero confirmation UI.**
  `JoinGroupScreen.tsx:25-59`, `JoinSessionScreen.tsx:24-48` — both call the join API
  unconditionally in a mount-time effect, before any group/session name or confirm step is shown.
  Since group invite links are permanent/regeneratable, a leaked/reposted/screenshotted link
  stays live and silently adds anyone who taps it (while logged in) to that group or session —
  session joins add the tapper as a live-money participant with no opt-in. Insert a "Join
  &lt;name&gt;?" confirmation between token receipt and the mutating call.
- **AppNavigator's hasSeenOnboarding state is never refreshed after onboarding completes** —
  read from storage once at mount (`AppNavigator.tsx:492-496`) and never again, while
  `OnboardingV2Screen.markSeen()` writes the completion flag to storage later in the same process
  lifetime with no way to notify the stale in-memory state. Concretely reproducible: a guest who
  completes onboarding, signs into an account, then logs out (all without killing the app) gets
  routed back to Welcome with `firstRun:true`, and choosing "Continue as guest" replays the whole
  onboarding funnel — reachable in production (`welcome`/`onboardingV2` both pinned ON), invisible
  to the existing test suite (only pure `entryRouting.ts` and isolated screens are tested, not the
  storage-write to routing-state wiring). Re-read storage (or lift a callback) instead of trusting
  the mount-time snapshot.
- **react-native-markdown-display pulls markdown-it/linkify-it versions with unpatched
  quadratic-complexity DoS advisories**, no upstream fix available (`fixAvailable: false`). Low
  exploitability today (only renders first-party lesson content); becomes directly relevant the
  moment any less-trusted text (AI Coach output, user content) is piped through the same
  Markdown.tsx component — flag before that happens, not after.
- **LandingScreen's purchase CTA has no internal paywall-flag guard**, unlike PaywallScreen
  (see the HIGH finding above — this is its enabling half). Give it the same self-guard.

### LOW

- **Expired pending invite is silently dropped with zero user feedback** on the guest-to-authed
  transition. `pendingInvite.ts`'s 15-minute TTL is genuinely enforced at read time (not a
  TOCTOU issue — server re-validates on join regardless) but the caller does nothing when the
  invite is null, whether missing or expired — no toast, no indication. Surface a "that invite
  expired" toast.
- **apps/landing's pinned Next.js (15.5.19) and postcss have High-severity advisories**
  (SSRF/DoS, path-traversal) fixed in later patch releases already within the existing semver
  ranges — but production impact is mitigated: the site is a static export (`output: 'export'`)
  with no running Next.js server, so the server-reachable advisories aren't exploitable in this
  specific deployment. Low-effort, low-risk `npm install` to pick up the patches regardless.
- **PokerApp.Tests transitively pulls a SQLitePCLRaw version with a High CVE**
  (CVE-2025-6965), no patched release exists yet upstream. Test-only (never ships), not urgent —
  track for a future patched release.
- **AI Coach's entry CTA and manual-submit button lack the "Demo" disclaimer** that the
  screenshot-entry path and the result screen already carry — a beta tester following the manual
  path sees no demo framing until after submitting. Add the same qualifier to both labels.

### INFORMATIONAL

- Dead Paddle web-billing provider injects the Paddle CDN script with no Subresource Integrity
  check — unreachable today (web payments are dead), add SRI if ever revived.
- Backend has no NuGet packages.lock.json (unlike both npm lockfiles, which are correctly
  committed) — transitive resolution isn't cryptographically pinned. The .gitignore already
  carves out an exception for this exact filename, suggesting it was the original intent.
- Backend Dockerfile pins base images by floating 8.0 tag, not a patch version or digest —
  low-risk (trusted first-party MCR images) but each Railway build can silently pick up a newer
  patch with no review step.
- 28 npm-audit findings (2 critical, 9 high) in apps/poker-mobile, but ALL concentrated in
  Expo CLI/Metro/Jest **devDependencies** — a dev-machine/CI supply-chain surface, not something
  Metro bundles into the shipped app. Periodic `npm audit fix` recommended, not urgent.

**What Wave 3 could NOT verify** (consolidated): none of the four agents ran the app or exercised
a live/deployed endpoint; live Railway/Vercel env values (whether billing keys, Coach API key, or
paywall were ever set differently than the committed defaults) aren't visible from this repo;
native iOS/Android build artifacts (CocoaPods/Gradle transitive deps, generated
Info.plist/AndroidManifest) weren't reviewed — this is a managed Expo workflow with no committed
native project files; no npm-audit/dotnet-vulnerable finding's real-world exploit availability
was independently researched beyond the advisory data itself; the entitlement-cache-bypass
finding is a static trace (onChoosePlan → purchase() → resolveActiveBillingProvider() →
mockBillingProvider.purchase() → saveEntitlement()), not an executed repro.

---

# Final combined report — by severity (2026-08-05)

Every OPEN finding across the whole document — the original 2026-08-03 partial run (minus the
5 items already ✅ RESOLVED, annotated inline above) plus all three fleet waves — unified onto
one scale (critical/high/medium/low/informational). **0 critical, 9 high, ~15 medium, ~12 low,
~15 informational.** Full evidence and fix text for every item lives in its wave section above;
this is the ranked index, with HIGH given full restatement since there are few enough to read in
one pass.

**One convergence worth flagging on its own:** the original 2026-08-03 audit flagged
`LessonReaderScreen.tsx:45`'s lock check as "latent — live the moment any new entry point
forgets the source-side check." Wave 3, run independently two days later with no memory of that
specific line, found the exploit path that makes it live TODAY: the entitlement resolver's
unsigned-cache fallback (below) makes `isPremium` return true without ever reaching a "new entry
point" — it just makes the EXISTING gate lie. Two independent passes, two days apart, converging
on the same root defect from different angles is a strong signal this one is real and not a
false positive.

## HIGH (9 — no criticals found across any dimension)

1. **AddPlayer consent bypass** — any authenticated user can attach any other registered user to
   a session's financial ledger with zero consent, generate real settlement debts against them,
   and the victim has no path to remove their own forced seat. `AddPlayerCommandHandler.cs:54-71`
   + unscoped `SearchUsersQuery`. *(Wave 1, auth/IDOR)*
2. **RedeemTopUp credit forgery** — grants paid AI credits with no purchase-receipt
   verification; dormant only because `TopUpSettings.Enabled` defaults false everywhere.
   `RedeemTopUpCommand.cs:36-52`. *(Wave 1, API/backend)*
3. **EndSession has no concurrency guard** — duplicate submits can double-count a player's
   cash-out, corrupting settlement totals. `EndSessionCommandHandler.cs:38-59`. *(Wave 1,
   live-money)*
4. **Deleted-account GUID survives forever, served live via the hand-history API** — not just a
   DB residue; `HandRecord.CreatedByUserId` has no modeled FK so it's invisible to the deletion
   ratchet, and `GetSessionHandHistoryQueryHandler` returns it verbatim. *(Wave 2, privacy)*
5. **Privacy policy's deletion promise is contradicted by actual handler behavior** — "all
   personal data removed immediately" is false for at least three field classes.
   `privacy.html:128-131`. *(Wave 2, privacy)*
6. **Analytics opt-out is not retroactive on re-opt-in** — events buffered during an opted-out
   session get sent in bulk once the user opts back in. Confirmed still live (first flagged
   2026-08-03). `analytics.ts:128-159`. *(Wave 2, privacy)*
7. **EndSession 500s on an already-successful session end if achievement evaluation throws** —
   the identical "best-effort side effect kills an already-succeeded critical-path response"
   defect class the two most-recent commits on this branch (PR #74, #78) just fixed elsewhere.
   `EndSessionCommandHandler.cs:72-98`. *(Wave 2, stats)*
8. **axios (the shared HTTP client, including auth token refresh) has a High-severity CVE**
   (GHSA-gcfj-64vw-6mp9), fix already within the existing semver range — just needs a lockfile
   regen. `package.json:26`. *(Wave 3, deps/supply-chain)*
9. **Entitlement resolver trusts an unsigned local cache on any server-call failure**, unlocking
   the full premium lesson library (already bundled unencrypted) with zero server verification —
   converges with the original audit's "latent" lesson-lock finding, above.
   `entitlementResolve.ts:25-37` + `LessonReaderScreen.tsx` (no self-check). *(Wave 3, premium)*

## MEDIUM (~15) — title, location, source

| Finding | Location | Source |
|---|---|---|
| Webhook endpoints unauthenticated, no rate limit, unbounded body read before signature check | `WebhooksController.cs:20-79` | Wave 1 |
| AddBuyIn auto-starts a Draft session, bypassing the admin/owner-only Start rule | `AddBuyInCommandHandler.cs:22-39` | Wave 1 |
| Buy-in/cash-out/hand-record authorization scoped to whole group, not session participants | `AddBuyInCommandHandler.cs` etc. | Wave 1 |
| Session invite-token redemption not atomic — two concurrent joins can consume one "single-use" token | `JoinSessionByTokenCommandHandler.cs:21-49` | Wave 1 |
| Bankroll AsyncStorage unscoped by user — data crosses login boundaries on shared devices | `bankrollStore.ts:20` | Wave 1 |
| Real JWT secret + DB password committed to git history (removed from tracking, still recoverable) | commits 49cf1ed→e6f0867 | Wave 2 |
| AchievementEvaluator identifies "just-ended session" by CreatedAt order, not the sessionId param | `AchievementEvaluator.cs:82-131` | Wave 2 |
| "XP is MONOTONIC" violated — 2 of 8 XP terms backed by user-deletable arrays | `EngagementContext.tsx:89-111` | Wave 2 |
| Group/session invite deep links auto-join with zero confirmation UI | `JoinGroupScreen.tsx:25-59` | Wave 3 |
| AppNavigator's hasSeenOnboarding goes stale, replays onboarding after guest→signin→logout | `AppNavigator.tsx:492-496` | Wave 3 |
| react-native-markdown-display pulls markdown-it/linkify-it with unpatched DoS advisories | `package.json` (react-native-markdown-display) | Wave 3 |
| LandingScreen's purchase CTA has no internal paywall-flag self-guard | `LandingScreen.tsx:136-146` | Wave 3 |
| 30s poll fetches the ENTIRE lifetime session payload just to find one active session | `ActiveSessionContext.tsx:25` | Original 2026-08-03, still open |
| Track hub ignores its segment param after first mount | `TrackScreen.tsx:50` | Original 2026-08-03, still open |
| Streak/freeze maintenance runs only at cold start, reminder scheduler reads stale value | `StudyContext.tsx:87` | Original 2026-08-03, still open |

## LOW (~12) — title, location, source

| Finding | Location | Source |
|---|---|---|
| Authenticated billing endpoints + ChangePassword have no rate limiting | `BillingController.cs:26-58`, `AuthController.cs:150-161` | Wave 1 |
| LogoutCommandHandler revokes any supplied refresh token without ownership check | `LogoutCommandHandler.cs:11-27` | Wave 1 |
| Settlement tie-break ordering differs between TS local engine and C# server engine | `local/settlements.ts:51-54` vs `SettlementCalculatorService.cs:9-19` | Wave 1 |
| AddPlayer's duplicate-guest-name check has a read-then-insert race | `AddPlayerCommandHandler.cs:36-52` | Wave 1 |
| Group/rivalry period filters bucket by session CreatedAt, not when actually played | `GetGroupLeaderboardQueryHandler.cs:25-37` | Wave 2 |
| Expired pending invite silently dropped, zero user feedback | `pendingInvite.ts:22-34` | Wave 3 |
| apps/landing's Next.js/postcss have High advisories, mitigated by static export | `package.json` (next, postcss) | Wave 3 |
| SQLitePCLRaw (test-only) has a High CVE, no upstream fix yet | `PokerApp.Tests.csproj:11` | Wave 3 |
| AI Coach entry CTA + manual-submit button lack the "Demo" disclaimer other paths carry | `SpotTrainerScreen.tsx:212-219`, `CoachInputScreen.tsx:156-160` | Wave 3 |
| Every non-Israeli user is locked to ₪ ILS (currencyPrefs OFF in prod) | `features.ts:46` | Original 2026-08-03, still open |
| Reminder prefs toggles show ON when nothing can fire; screen has no toast despite CLAUDE.md's claim | NotificationPreferencesScreen | Original 2026-08-03, still open |
| Account-deletion refusal for group owners names a transfer endpoint the API doesn't expose | `DeleteAccountCommandHandler.cs:24-26` | Original 2026-08-03 deferred finding, re-confirmed by Wave 2 |

## INFORMATIONAL (~15)

CSP is report-only not enforced; JwtService re-derives its signing key instead of routing through
JwtKey.ResolveSigningKey; Apple Sign-In skips nonce check when omitted; GroupRole.Owner vs
Groups.OwnerId divergence — verified NOT reachable; Expo push service is an undisclosed
third-party recipient of other users' usernames; "18+" claim has zero technical enforcement; no
automated data-export endpoint; GetGroupRivalsQuery shows pairwise P&L to non-co-players;
CLAUDE.md's invite-link table says "Permanent," code says 7 days; LeaveGroup's XML doc comment
contradicts actual (correct) behavior; RemoveMember doesn't touch historical SessionPlayer
attribution; dead Paddle CDN script has no SRI; backend has no NuGet packages.lock.json; Dockerfile
pins a floating `8.0` tag; 28 npm-audit findings concentrated entirely in devDependencies. Full
detail for every item above in its wave section.

## What this report does NOT cover

Per-dimension `notVerified` lists are preserved in full in each wave's section above — read them
before treating silence on any specific mechanism as a clean bill of health. The recurring
themes: no agent ran the app or hit a live/deployed endpoint (static reading only, by design);
several concurrency-race findings are argued from code tracing, not an executed reproduction;
live Railway/Vercel environment variable values aren't visible from this repo; native
iOS/Android build artifacts weren't reviewed (managed Expo workflow, no committed native
projects); the git-history secret scan was sampled, not exhaustive.

## Suggested triage order (not a plan — the owner decides)

The 9 HIGH findings split into two shapes: **five are pure code fixes** (RedeemTopUp verification,
EndSession concurrency, HandRecord.CreatedByUserId cleanup, analytics opt-out fix, EndSession
try/catch) that don't require a product decision — each could be its own small PR, same pattern
as PRs #75-78. **Two require an explicit product/legal decision first** (AddPlayer consent —
route through invites or add a pending-accept state; privacy policy wording — fix the gap or
rewrite the promise). **Two are urgent but mechanical** (axios lockfile bump; entitlement cache
fix + LessonReaderScreen self-check + LandingScreen paywall guard, ideally as one slice since
they're the same root cause).

---
