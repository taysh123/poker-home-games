# T Poker — iOS Release Readiness

_Last verified: June 17, 2026 against the live codebase. Publisher (seller of record): **Tay Shofer**._

## Status: ✅ BUILD-READY · submission gated only by external Apple actions

The app builds into a production `.ipa` today. No code or config defects remain.

---

## 1. Expo configuration (`apps/poker-mobile/app.json`)

| Item | Value | Verdict |
|---|---|---|
| App name | `T Poker` | ✅ |
| Slug / owner / projectId | `t-poker` / `taysh` / `8380f2f3-8dbb-478e-b92f-c39e3ebb3d8b` | ✅ |
| Marketing version | `1.1.0` | ✅ |
| Build number | managed remotely (`appVersionSource: remote` + `autoIncrement`) | ✅ |
| iOS bundle identifier | `com.tpoker.app` | ✅ |
| Scheme / deep links | `tpoker` + `applinks:poker-home-games-three.vercel.app` | ✅ |
| Icon | `./assets/icon.png` 1254×1254 no-alpha (Expo downscales to 1024²) | ✅ |
| Splash | `./assets/splash-icon.png`, bg `#0A111B` | ✅ |
| Export compliance | `ITSAppUsesNonExemptEncryption: false` | ✅ |
| Permissions | only `expo-notifications` (push) — no camera/photos/location/contacts/mic | ✅ no usage strings required |

## 2. EAS configuration (`apps/poker-mobile/eas.json`)

- `build.production`: `autoIncrement: true`, store distribution (default), env vars baked
  (`EXPO_PUBLIC_API_URL`, `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID`) → produces device/store `.ipa`.
- `cli.appVersionSource: remote` — EAS manages build numbers.
- `submit.production.ios`: **`appleTeamId: J2MGQU5C7U`**, **`ascAppId: 6781109023`** (set June 17, 2026).

## 3. Apple deployment readiness

- ✅ Production `.ipa` pipeline valid (same pipeline already produces Android builds).
- ✅ No Expo package blocks submission (standard Expo/RN modules only; no ad/tracking SDK).
- ⏳ Apple signing credentials created interactively via `eas credentials` (needs Apple
  Developer Program enrollment + Apple ID 2FA).

## 4. Privacy & compliance

Collected (account users only; guest mode transmits nothing): email, username, user IDs,
game records (user-generated content), Expo push token. No tracking/analytics/ads SDK.
iOS privacy manifest auto-generated by SDK 54. Full mapping: [data-safety.md](data-safety.md).

**App Store Connect → App Privacy:**
| Apple data type | Linked | Tracking | Purpose |
|---|---|---|---|
| Contact Info → Email Address | Yes | No | App Functionality |
| Identifiers → User ID | Yes | No | App Functionality |
| Identifiers → Device ID (push token) | Yes | No | App Functionality |
| User Content → Other User Content (game records) | Yes | No | App Functionality |

## 5. Build readiness

| Check | Result |
|---|---|
| `npx expo-doctor` | ✅ 18/18 |
| `npx tsc --noEmit` | ✅ 0 errors |
| `npx jest` | ✅ 59/59 |
| SDK compatibility | ✅ SDK 54 (expo 54.0.35, RN 0.81.5, React 19.1) |
| Missing assets / crashes | ✅ none |

## 6. Release deliverables

`eas.json` ✅ · `app.json` ✅ · bundle id `com.tpoker.app` ✅ · production build profile ✅ ·
App Store submit profile ✅ (Team ID + ASC App ID set) · EAS Submit config ✅.

## 7. Remaining external actions

1. Apple Developer Program enrollment ($99/yr).
2. App record in App Store Connect (bundle `com.tpoker.app`) — ASC App ID `6781109023`.
3. iOS signing credentials via `eas credentials` (interactive).
4. Optional: APNs key for live push; iOS Google OAuth client for `com.tpoker.app`.

## 8. Commands (from Windows, in `apps/poker-mobile`)

```powershell
eas credentials                                   # iOS -> let EAS manage cert + profile
eas build --profile production --platform ios     # cloud build -> .ipa
eas submit --platform ios --latest                # upload to App Store Connect / TestFlight
```

**No Mac required** — EAS builds on cloud macOS workers and submits over the network.
